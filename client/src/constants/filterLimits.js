// Upper bound for runtime sliders (minutes). Used by TMDB and IMDb filter panels.
// Treated as "no cap" sentinel — values equal to this are omitted from outgoing filters.
export const RUNTIME_MAX_MINUTES = 400;
